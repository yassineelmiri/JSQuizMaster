<div class="breadcrumb-area" style="background-image:url('assets/img/other/3.png'); height: 300px; margin: 0 auto;">
    <div class="container">
        <div class="breadcrumb-inner">
            <div class="section-title mb-0 text-center">
                <h2 class="page-title">Sign In</h2>
                <ul class="page-list">
                    <li><a href="home.html">Home</a></li>
                    <li>Sign In</li>
                </ul>
            </div>
        </div>
    </div>
</div>
<div class="signin-page-area pd-top-120 pd-bottom-90 ">
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-xl-6 col-lg-7">

                    <form action="/logined" method="post" class="row g-3 needs-validation signin-inner" novalidate id="loginForm">
                    <div class="row">
                        <div class="col-12">
                            <div class="single-input-inner style-bg-border">
                                <input placeholder="Email" type="email" class="form-control" name="email" id="email" require>
                            </div>
                        </div>
                        <div class="col-12">
                            <div class="single-input-inner style-bg-border">
                                <input type="password" placeholder="password..." class="form-control" name="password" id="password" required>
                            </div>
                        </div>
                        <div class="col-12 mb-4">
                            <button class="btn btn-base w-100" type="submit">Sign In</button>
                        </div>
                        <div class="col-12">
                            <a href="#">Forgottem Your Password</a>
                            <a href="/register"><strong>Don't have an account?</strong></a>
                        </div>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

<script>
    document.addEventListener('DOMContentLoaded', function () {
        const form = document.getElementById('loginForm');

        form.addEventListener('submit', function (event) {
            let isValid = true;

            const email = document.getElementById('email');
            const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
            if (!emailRegex.test(email.value)) {
                email.classList.add('is-invalid');
                isValid = false;
            } else {
                email.classList.remove('is-invalid');
                email.classList.add('is-valid');
            }

            const password = document.getElementById('password');
            const passwordRegex = /^(?=.*[A-Za-z])[A-Za-z]{6,}$/;

            if (!isValid) {
                event.preventDefault();
                event.stopPropagation();
            }

            form.classList.add('was-validated');
        });
    });
</script>
